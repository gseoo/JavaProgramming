package days21;

public class Ex12 {
	public static void main(String[] args) {
		/*
		 [JCF]
		 = Java Collection Framework
		 = 컬렉션 프레임워크
		 
		 1. 프레임워크(framework) : 표준화된 체계 개발환경
		 > 다수의 데이터를 쉽고 효과적으로 처리할 수 있는 표준화된 방법을 제공하는 클래스의 집합
		 >> 자료 구조와 알고리즘을 구조화하여 클래스로 구현해 놓은 것
		 
		 2. 컬렉션(Collection) : 데이터의 집합
		 3. 컬렉션 프레임워크 : 다수의 데이터의 집합을 관리하는 표준화된 체계
		 
		 4. [컬렉션 프레임워크의 핵심 인터페이스(3가지 타입)]
		 컬렉션 인터페이스를 물려받음
		 컬렉션 클래스 : 해당 인터페이스들을 구현한 클래스
		 1) List : 순서 O, 중복 O 
		 			ArrayList, LinkedList, Stack, Vector 등
		 2) Set : 순서 X, 중복 X
		 			HashSet, TreeSet 등
		 3) Map : (key, value) 한 쌍 == Entry(엔트리)
		 			key : 순서 X, 중복 X/ value : 순서 X, 중복 O
		 			HashMap, HashTable, TreeMap, Properties 등
		 */
	}
}
